# Claude Code skills (mirror)

Skills in this folder mirror **`.cursor/skills/`** at the repo root so Claude Code loads the same workflows.

Edit skills in **`.cursor/skills/`**, then copy changes here (or re-copy the folder) to keep both in sync.
